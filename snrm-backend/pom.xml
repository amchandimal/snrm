<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.13</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.snrm</groupId>
	<artifactId>snrm-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>snrm-backend</name>
	<description>SNRM backend — Supply Network Resilience Modelling tool (design: ../SNRM_Software_Design_Document.md)</description>
	<properties>
		<java.version>21</java.version>
		<springdoc.version>2.6.0</springdoc.version>
		<mapstruct.version>1.6.3</mapstruct.version>
		<poi.version>5.2.5</poi.version>
		<commons-csv.version>1.10.0</commons-csv.version>
		<jgrapht.version>1.5.2</jgrapht.version>
	</properties>
	<dependencies>
		<!-- Web / API -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<!--
		  JWT bearer authentication.

		  Spring Security's own resource-server starter rather than a third-party JWT library:
		  Spring Security is what the stack carries for this concern and nothing else, and the
		  starter brings both halves of the flow — NimbusJwtEncoder to mint the token in
		  auth/JwtService and NimbusJwtDecoder plus the BearerTokenAuthenticationFilter to verify
		  it on every other request. Adding jjwt would mean a library outside the stack.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>

		<!-- DTO boundary: entities never cross the API. Processor is in build/plugins. -->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>

		<!-- Persistence (MySQL 8.x + Flyway) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
		  Graph library: proven implementations of centrality, connectivity,
		  shortest paths on directed weighted graphs.

		  jgrapht-core alone. The NetworkGraph snapshot *is* a JGraphT
		  directed weighted graph, and the topological suite runs on it —
		  maximum flow for NODE_CRITICALITY above all, which is the one piece here that
		  would be a genuine mistake to hand-roll. The io/ext modules would only add
		  file formats this project already has adapters for.
		-->
		<dependency>
			<groupId>org.jgrapht</groupId>
			<artifactId>jgrapht-core</artifactId>
			<version>${jgrapht.version}</version>
		</dependency>

		<!--
		  CSV / XLSX network import and export.

		  poi-ooxml carries both halves of the Excel path: XSSFReader with the SAX
		  event model for the streaming read the import path needs, and SXSSFWorkbook for
		  the streaming write of the matching export. commons-csv parses the CSV form
		  with the delimiter auto-detection that same path requires.
		-->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>${poi.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-csv</artifactId>
			<version>${commons-csv.version}</version>
		</dependency>

		<!--
		  No search library, deliberately. The legacy reverse-auction PoC that pulled in the
		  two GA/maths libraries is deleted, and they went with it. Jenetics is the choice
		  for Phase 2 search; it is added when the ConfigurationStrategy SPI is written against
		  it, not carried unused until then.
		-->

		<!-- Test (JUnit 5 + Testcontainers-MySQL) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mysql</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!--
			  MapStruct generates the *Mapper implementations at compile time. The
			  processor is declared on the processor path rather than as a dependency so it never
			  reaches the runtime classpath. `-parameters` is inherited from the Boot parent and
			  must stay: Spring needs it to bind constructor arguments of the DTO records.
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${mapstruct.version}</version>
						</path>
					</annotationProcessorPaths>
					<compilerArgs>
						<arg>-Amapstruct.suppressGeneratorTimestamp=true</arg>
						<arg>-Amapstruct.defaultComponentModel=spring</arg>
						<arg>-Amapstruct.unmappedTargetPolicy=WARN</arg>
					</compilerArgs>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>

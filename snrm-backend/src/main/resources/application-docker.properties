# 'docker' — app and MySQL both in docker-compose: docker compose up --build -d
# Resolves the compose service name on the internal network, so the container port (3306) applies.
spring.datasource.url=jdbc:mysql://mysql:3306/snrm
spring.datasource.username=snrm
spring.datasource.password=snrm
